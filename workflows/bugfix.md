# Bugfix Workflow

Use for reported defects, failing tests, and regressions.

1. Activate `systematic-debugging`.
2. Reproduce or document the failure evidence.
3. Isolate the root cause.
4. Add a regression test when practical.
5. Implement the smallest fix.
6. Use `verification-gate` before completion.
